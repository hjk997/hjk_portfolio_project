<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.AdminLogin">
    <select id="selectUser" parameterType="String" resultType="com.hjkportfolio.hjk.user.AdminBean">
        select * from admin where admin_id = #{id};
    </select>
</mapper>